# Dev test notes
## Running the tests

Tests are run using `pytest` command within this directory. You can run the tests individually using `pytest <test_file>.py` or run all tests in the directory using `pytest`.